# Nosana Production Environment Variables
# Copy this file to nosana.env and fill in your actual values
# DO NOT commit nosana.env to git!

# OpenAI API Key
OPENAI_API_KEY=your-openai-api-key

# Google OAuth Credentials (use your own client)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# IMPORTANT: Update these URLs after you deploy (e.g., https://xxxxxx.node.k8s.prd.nos.ci)
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google Refresh Token (obtain via OAuth flow, see README)
GOOGLE_REFRESH_TOKEN=your-google-refresh-token

# Database URL (keep as is for SQLite in Docker)
DATABASE_URL=file:./prisma/data/prod.db

# Node Environment
NODE_ENV=production

# Auth Bypass (for hackathon demo/testing)
# Set to 'true' to allow access without OAuth - app will use the configured GOOGLE_REFRESH_TOKEN
# This allows judges to access the app with your real integrated data
NEXT_PUBLIC_BYPASS_AUTH=true

